{
  "name": "@codori/server",
  "version": "0.13.6",
  "private": false,
  "description": "Codori server for Git project discovery, Codex runtime management, and bundled dashboard serving.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "codori-server": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/comfuture/codori.git",
    "directory": "packages/server"
  },
  "homepage": "https://github.com/comfuture/codori#readme",
  "bugs": {
    "url": "https://github.com/comfuture/codori/issues"
  },
  "engines": {
    "node": ">=22.22.2"
  },
  "keywords": [
    "codori",
    "codex",
    "remote-coding",
    "nuxt",
    "fastify"
  ],
  "files": [
    "dist",
    "client-dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node ./scripts/sync-client-bundle.mjs",
    "lint": "eslint src test --ext .ts",
    "prepack": "pnpm --dir ../client build && pnpm --dir ../webxr build && pnpm run build",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.0",
    "@fastify/static": "^10.1.3",
    "@fastify/websocket": "^11.3.0",
    "@openai/codex": "^0.147.0",
    "fastify": "^5.11.2",
    "image-size": "^2.0.2",
    "mime-types": "^3.0.2",
    "ora": "^9.4.1",
    "picocolors": "^1.1.1",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/mime-types": "^3.0.1"
  }
}
